# Agent Notes

Project documentation lives in [CLAUDE.md](CLAUDE.md): tech stack, design tokens, content-editing guide, and deployment steps.

Content changes belong in `src/content/` (Markdown) and `src/data/profile.ts` — not in components or pages.

Dev server: `npm run dev`; production build: `npm run build` → `dist/`.
